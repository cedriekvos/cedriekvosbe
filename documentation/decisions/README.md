# Architecture Decision Records

The ADRs live here as HTML pages — **open [`index.html`](index.html) in a
browser** for the index, the conventions, and the house style. GitHub renders
`.html` as source, so read them from a local copy
(`open documentation/decisions/index.html`) or a served copy of the docs.

This file is a signpost only. It holds no decisions and no conventions — those
belong on the pages themselves.
